package com.generic.androidtracker.interfaces;

public interface OnShipmentListener {
    /**
     * Handles the action event when a shipment item view holder is clicked on.
     * @param position
     */
    void onShipmentClicked(int position);
}
